Para evitar conflitos com arquivos de compilações anteriores, limpe o diretório antes de começar:

make clean

Depois já podemos iniciar o xv6 no emulador QEMU usando:

make qemu

Quando a inicialização terminar, basta rodar o comando:

testsched
